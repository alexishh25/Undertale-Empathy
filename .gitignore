# ==========================
# Godot 4 + Mono C# .gitignore
# ==========================

# --- Godot ---
# Carpetas de importación y exportación (generadas automáticamente)
.import/
.export/

# Configuración y cache local del editor
.godot/

# Ignorar metadatos UID de scripts
*.uid

# Archivos generados por Godot que no deben versionarse
*.import
*.tmp
*.translation

# Presets de exportación (pueden contener rutas locales)
export_presets.cfg

# --- Mono / .NET ---
# Carpetas de compilación
/bin/
/obj/

# Archivos temporales de bloqueo de compilación
*.pidb
*.mdb

# Archivos de usuario (configuración local del IDE)
*.user
*.userprefs
*.suo
*.csproj.user

# --- Visual Studio ---
.vs/
.idea/                # Para JetBrains Rider
*.svd

# Archivos de caché y temporales de Rider/VSCode
.vscode/
*.orig

# --- NuGet ---
# Paquetes descargados (normalmente se restauran con dotnet restore)
packages/
# Archivos de lock (opcional, quitar si quieres que se controle la versión exacta)
# *.lock.json

# --- Sistema operativo ---
# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db
desktop.ini

# Linux
*~

# ==========================
# Notas:
# - NO ignores .csproj, .sln ni archivos .gd, .tscn, .tres ya que son parte del proyecto.
# - "export_presets.cfg" se ignora para evitar rutas locales, pero si trabajas en equipo y tienen presets comunes, inclúyelo.
# ==========================
